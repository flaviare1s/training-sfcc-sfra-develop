<div class="custom-banner">
    <a href="${pdict.categoryURL}">
        <img src="${pdict.bannerImage}" alt="Banner Image" />
        <isprint value="${pdict.bannerImage}" />
        <isprint value="${pdict.categoryURL}" />
        ${'teste'}
    </a>
</div>
